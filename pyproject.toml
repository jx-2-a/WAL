[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "wal"
version = "0.1.0"
description = "WAL — 小说写作 Agent 系统"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["wal*"]
